# For SQLite (dev):      sqlite:///./cadrebook.db
# For PostgreSQL (prod): postgresql://user:password@localhost:5432/cadrebook
DATABASE_URL=postgresql://user:password@localhost:5432/cadrebook

# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-me-to-a-long-random-secret-generate-with-command-above

ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
